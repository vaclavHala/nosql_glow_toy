[
  '{{repeat(10000)}}', 
  {
    _id: function (tags, index) {
      return index;
    },
    description: "{{lorem()}}",
    name: "{{lorem(1, 'words')}}",
  offers: {
    availability: "{{random('InStock', 'Discontinued', 'InStock', 'InStoreOnly', 'LimitedAvailability', 'OnlineOnly', 'OutOfStock', 'PreOrder', 'SoldOut' )}}",
    price: "{{floating(1,10000,2)}}",
    priceCurrency: "USD"
  }
}
]

\{\s*"_id": ([0-9])+,\s*"description": "([a-zA-Z. ]+)",\s*"name": "([a-zA-Z]+)",[^a]*availability": "([a-zA-Z]+)",\s*"price": ([1-9.]+),\s*"[^}]*\},?
with s and g modifiers
